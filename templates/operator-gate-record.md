# Operator Gate Record

Private/operator-owned record. Do not publish concrete provider account IDs,
pod IDs, volume IDs, billing identifiers, credential names with sensitive
meaning, signed URLs, raw logs, private paths, license files, or private data.
Public copies should use aliases only.

- issue:
- campaign:
- requested action:
- provider:
- manifest:
- code ref:
- image digest:
- network volume alias:
- max spend:
- max runtime:
- cleanup policy:
- expected artifacts:
- validation commands:
- data posture:
- license gates:
- explicit authorization:
- authorization timestamp:
- operator:
- notes:
